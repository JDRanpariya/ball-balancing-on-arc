# Fine-tuning TD3 config - reduced LR for real hardware adaptation
_target_: stable_baselines3.TD3
policy: "MlpPolicy"
device: "cpu"
learning_rate: 0.0001
batch_size: 256
learning_starts: 0    # no warmup - we already have a pretrained model
