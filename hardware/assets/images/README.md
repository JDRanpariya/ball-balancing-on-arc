# Reference Photos

Photos of the physical build.

- `system_setup.jpg` - full setup (actuator + arc + sensors), captured from
  an MPPI success run with the ball balanced at arc centre.
- `plc_closeup.jpg` - DIN-rail terminal stack (Beckhoff EL-terminals + IPC).
