# Pinned reference environment for the ball-on-arc benchmark (pip mirror of env.yaml).
# These are the exact versions that produced the released tables, figures, and
# checkpoints, captured from a working environment (Python 3.11 target; the
# pins were verified on the reference machine running these package versions).
#
#   python -m pip install -r requirements.lock.txt
#   pip install -e balancer/
#
# This locks the direct, numerically load-bearing dependencies. For a fully
# byte-reproducible transitive lock, regenerate on the target platform with
# `pip freeze` inside a fresh env built from env.yaml.

numpy==2.2.6
scipy==1.16.0
matplotlib==3.10.3
pandas==2.3.3
seaborn==0.13.2
h5py==3.14.0
PyYAML==6.0.3
scikit-learn==1.7.0
tqdm==4.67.1
torch==2.10.0
gymnasium==1.0.0
stable-baselines3==2.9.0
sb3-contrib==2.9.0
moviepy==2.1.2
hydra-core==1.3.4
omegaconf==2.3.1
wandb==0.24.2
tensorboard==2.21.0
rich==13.9.4
osqp==1.1.3
casadi==3.7.2
d3rlpy==2.8.1
cma==4.4.4
joblib==1.5.1
pyserial==3.5
onnxruntime==1.27.0
pytest==9.1.1
# pygame is an optional 'render' extra only (balancer/[all]); not required to
# reproduce any paper result. Install separately if you need on-screen rendering.
